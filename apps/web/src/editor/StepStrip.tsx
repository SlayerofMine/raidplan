/**
 * Bottom strip (plan §1.1). A placeholder in Phase 1 — steps, thumbnails, and
 * reordering arrive with the animation model in Phase 3.2.
 */
export function StepStrip() {
  return (
    <footer className="flex items-center gap-2 border-t border-panelborder bg-panel px-3 py-2 text-sm text-neutral-500">
      Steps — coming in Phase 3.
    </footer>
  );
}
