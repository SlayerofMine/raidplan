# Keep native modules (e.g. better-sqlite3 in Phase 4) building against the
# host arch — never copy node_modules between x64 and aarch64 (plan §14).
node-linker=isolated
strict-peer-dependencies=false
